use ZergSpawningPool as knowledge.
use ZergSpawningPool as actionspec.

module ZergSpawningPool {
	if a-goal(upgrade('Metabolic Boost')) then upgrade('Metabolic Boost').
	
	if a-goal(upgrade('Adrenal Glands')) then upgrade('Adrenal Glands').	
}