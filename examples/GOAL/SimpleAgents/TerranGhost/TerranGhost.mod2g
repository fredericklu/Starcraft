use TerranGhost as actionspec.
use TerranGhost as knowledge.

module main{

		if percept(isBeingConstructed) then skip.
		if percept(unit("Terran Siege Tank Tank Mode", B), unit("Terran Marine", A)), bel(A > 30,B > 3 , 
		enemyBase(X,Y)) then attack(X, Y).
}

