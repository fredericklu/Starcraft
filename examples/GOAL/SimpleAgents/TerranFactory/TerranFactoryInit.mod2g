use TerranFactory as knowledge.

module TerranFactoryInit {
	if percept(friendly(_,"Terran Command Center",_,_,_,_,_,X,Y)) then insert(commandCenter(X,Y)).
		forall percept(chokepoint(X,Y)) do insert(chokepoint(X,Y)).
}