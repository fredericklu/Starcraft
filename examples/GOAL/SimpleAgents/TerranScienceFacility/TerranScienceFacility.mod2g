use TerranScienceFacility as actionspec.
use TerranScienceFacility as knowledge.

module main{

		if percept(isBeingConstructed) then skip.
		if percept(not(addon("Terran Covert Ops")), queueSize(N), minerals(X), gas(Y)), bel(N = 0, X>=50, Y>=50) 
			then buildAddon("Terran Covert Ops").
}