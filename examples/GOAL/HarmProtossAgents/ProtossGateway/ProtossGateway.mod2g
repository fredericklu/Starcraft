use ProtossGateway as actionspec.
use ProtossGateway as knowledge.

module main{
		if percept(isBeingConstructed) then skip.
		if percept(rallyPoint(RX,RY)), bel(RX = 0, RY = 0, commandCenter(X,Y), rallyLocation(X,Y,NX,NY)) then setRallyPoint(NX,NY).	
		if a-goal(train(Name)) then train(Name) + drop(train(Name)).
}

	%beliefs{
	%	trainMarine.
	%	trainMedic.}
	