use TerranMarine as knowledge.

define attack(X,Y) with
	pre{true}
	post{true}

		
define move(X,Y) with
	pre{true}
	post{true}
		
		
define attack(Id) with
	pre{true}
	post{true}

define ability(Id) with
	pre{true}
	post{true}
		
define skip with
	pre {true}
	post {true}	
	
define rightClick(Id) with
	pre {true}
	post {true}
