use ZergHydralisk as knowledge.
%use AttackModule.AttackKnowledge as knowledge.

module ZergHydraliskInit {
	if percept(resources(M, G, _, _)) then insert(minerals(M), gas(G)).
	forall percept(base(X, Y, Begin, RegionID)) do insert(base(X, Y, Begin, RegionID)).
	forall percept(chokepoint(X,Y)) do insert(chokepoint(X,Y)).
	if percept(status(_, _, X, Y)) then insert(position(X,Y)).
	%if bel(position(X,Y), rallyLocation(X,Y,NX,NY)) then adopt(move(NX,NY)).	
}