main module{
	
	program{
		if bel(percept(friendly(_,'Terran Marine', Id,_, _, _, _, _, _))) then follow(Id).
		
	}	

	actionspec {
		follow(Id){
			pre{not(percept(following))}
			post{true}
		}
		
	}
}