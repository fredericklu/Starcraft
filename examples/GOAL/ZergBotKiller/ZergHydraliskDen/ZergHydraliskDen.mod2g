use ZergHydraliskDen as knowledge.
use ZergHydraliskDen as actionspec.

module ZergHydraliskDen {
	if a-goal(upgrading('Muscular Augments')) then upgrade('Muscular Augments').
	
	if a-goal(upgrading('Grooved Spines')) then upgrade('Grooved Spines').	
	
	if a-goal(hasResearched('Lurker Aspect')) then research('Lurker Aspect').
}