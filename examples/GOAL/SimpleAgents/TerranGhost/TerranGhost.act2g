use TerranGhost as knowledge.

define attack(X,Y) with
	pre{true}
	post{true}
		
		
define move(X,Y) with
	pre{true}
	post{true}
		
		
define attack(Id) with
	pre{true}
	post{true}
		
define _use(Id) with
	pre{true}
	post{true}

define skip with
	pre {true}
	post {true}	
