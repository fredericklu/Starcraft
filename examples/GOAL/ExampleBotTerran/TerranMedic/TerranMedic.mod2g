use TerranMedic as actionspec.
use "../GenericMods/GenericPredicates" as knowledge.

module main{
	
	% If goal to follow and nobody is following him yet, start following
	if a-goal(following(Id)) then follow(Id) + drop(following(Id)).
	
}
